# FORGET.md — Feelings-Core 待修复项

> 扫描日期：2026-06-01
> 范围：架构设计（零代码）
> 实现语言：Anim v2.0+（自举后——和 animi 同语言，共享类型系统）
> 注意：Feelings-Core 不是独立项目。Core = animi 的 Pass 6-8（Personalize/DeviceMap/CodeGen）。
>       Pass 0-5 已在 Anim v1.0 完成（FSIR JSON）。Core 是同一个二进制里的后半段——不是新东西。
> 原则：P0 = 生产命门。P1 = 功能受限。

---

## P0 — 生产命门（0/4）

1. **PBM 加载/持久化零代码** — 个人基线矩阵的本地存储与 Session 恢复未实现。FIXME: v0.3。

2. **Session 管理零代码** — Session 启动/暂停/结束/历史记录未实现。FIXME: v0.3。

3. **安全看门狗零代码** — Session 运行中用户状态突变的 10Hz 轻量重校验未实现（FSIR 缓存版本校验 + 创伤状态变更 + cap 降级）。FIXME: v0.3。

4. **经线冷却保护零设计** — 经线建立后需要恢复期，不能连续焊、不能连续扯。

   问题：Feelings 将"爱上"经线建立的时间从几个月压缩到几帧。没有冷却保护——用户可在数小时内对多人产生真实的归属感——PBM 反复焊丝又撕裂——每一根断丝都在身体里空转——最终碎成片段。

   需求：
   - 每次经线建立（Session 中 PBM 偏移超过阈值）→ 启动冷却计数器
   - 冷却期内新经线的建立阈值显著提高（不是禁止，是难度陡增）
   - 冷却时间与上一段交叉的深度正相关
   - 用户可见：当前冷却状态、剩余冷却时间、不隐含评判

   不是"不准再爱。"是"让 PBM 有足够的时间消化上一次的偏移再开始下一次。"

   FIXME: v0.4。

---

## P1 — 功能受限（0/3）

5. **个人基线收敛跟踪零代码** — PBM 四维偏移的收敛趋势检测未实现。

6. **生理数据预处理管线零代码** — 原始 HRV/EDA/EEG → PBM 输入格式的转换未实现。

7. **设备数据融合零代码** — 多设备生理数据的同步与加权未实现。

---

## 编辑记录

```
2026-06-01  v0.1 初始扫描
            - 架构规范 100%
            - 代码 0%
            - P0 4 项：PBM 持久化 / Session 管理 / 安全看门狗 / 经线冷却保护
            - P1 3 项：收敛跟踪 / 预处理管线 / 数据融合
```